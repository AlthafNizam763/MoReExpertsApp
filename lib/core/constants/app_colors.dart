import 'package:flutter/material.dart';

class AppColors {
  // Primary Monochromes
  static const Color black = Color(0xFF212121);
  static const Color white = Color(0xFFFFFFFF);

  // Grays for nuances
  static const Color darkGray = Color(0xFF1A1A1A);
  static const Color mediaGray = Color(0xFF808080);
  static const Color lightGray = Color(0xFFF5F5F5);

  // Status Colors (kept minimal/muted to maintain premium feel)
  static const Color success = Color(0xFF2E7D32); // Muted green
  static const Color warning = Color(0xFFED6C02); // Muted orange
  static const Color error = Color(0xFFD32F2F); // Muted red
  static const Color info = Color(0xFF0288D1); // Muted blue

  // Interaction
  static const Color splashColor = Color(0x1AFFFFFF); // Low opacity white
  static const Color primaryBlue =
      Color(0xFF1b72b5); // Blue for buttons and active states
  static const Color borderGray =
      Color(0xFFE0E0E0); // Gray for borders and dividers
}
